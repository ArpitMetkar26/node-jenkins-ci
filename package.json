{
  "name": "node-jenkins-ci",
  "version": "1.0.0",
  "description": "Node.js app for Jenkins CI pipeline",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo Build completed"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  }
}

